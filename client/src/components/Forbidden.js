/**
 * Displays a message letting the user know they can't access the requested page.
 * @returns Forbidden page display
 */

const Forbidden = () => (
    <div className="wrap">
        <h2>Forbidden</h2>
        <p>Oh oh! You can't access this page.</p>
    </div>
);

export default Forbidden;